package com.micatechnologies.minecraft.csm.parks.trees;

/**
 * Marker for a leaves block of the tree kit. A log reaches into leaves it carries (the leader
 * into the crown above, a limb into the cluster at its end), and leaves shape their fringe
 * against one another; vanilla leaves count as leaves for both.
 *
 * @since 2026.9
 */
public interface ICsmTreeLeaves {
}
