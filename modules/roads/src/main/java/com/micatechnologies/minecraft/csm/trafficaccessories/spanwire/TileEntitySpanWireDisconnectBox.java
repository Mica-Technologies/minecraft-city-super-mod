package com.micatechnologies.minecraft.csm.trafficaccessories.spanwire;

/**
 * The disconnect box's tile entity.
 *
 * <p>A hanger in every respect -- it clamps to the messenger and gets the same mast, saddle and
 * coils -- so it holds nothing of its own. It exists as a distinct type only so the box can be
 * registered as its own block and told apart from a signal mount.
 */
public class TileEntitySpanWireDisconnectBox extends TileEntitySpanWireHanger {
}
