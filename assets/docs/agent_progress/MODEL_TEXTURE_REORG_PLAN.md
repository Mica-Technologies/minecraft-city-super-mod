# Model & Texture Organization — Progress & Plan

Reorganize all block models, shared models, and textures into a consistent folder
structure based on subsystem/tab categories. Rename models and textures to descriptive
lang-derived names.

**Created:** 2026-03-31
**Status:** Phase 2-3 substantially complete. All 10 subsystems organized.

---

## Resume Prompt

> The progress document is at `assets/docs/agent_progress/MODEL_TEXTURE_REORG_PLAN.md`.
>
> **All 10 subsystems organized.** 798+ block models moved to subsystem folders with
> lang-derived descriptive names. 451 sign textures in trafficsigns/ with lang names.
> Cross-subsystem shared textures in shared_textures/. Build verified after each step.
>
> **Remaining:** 517 flat variant models (fence_post, slab_top, stairs_inner/outer —
> auto-generated by AbstractBlockSetBasic), 30 flat textures, and ~261 textures in
> shared_textures/ that could potentially be further organized.
>
> **Tools:** AssetDependencyTracerTool in dev-env-utils traces the full
> block→blockstate→model→texture chain. Report at asset_dependency_report.json.

---

## Completed Work (2026-03-31)

### Phase 1: Dependency Tracing (COMPLETE)

- [x] Built `AssetDependencyTracerTool` in dev-env-utils
- [x] Generated full dependency report (1,313 blocks traced)
- [x] Mapped blocks to subsystems via tab files
- [x] Identified 62 UNKNOWN blocks (metal fence/slab/stairs → buildingmaterials)
- [x] Identified 59 CsmTabNone blocks (retiring signals → trafficsignals)

### Phase 2: Organize Block Models (COMPLETE for all subsystems)

All block models moved from flat `models/block/` to subsystem subdirectories with
descriptive lang-derived names.

| Subsystem | Models Moved | Rename Examples |
|---|---|---|
| HVAC | 30 | art1→air_return_1_white, sv3→four_way_vent_3_white |
| Novelties | 17 | acpacman→arcade_pacman, hd→hand_dryer |
| Building Materials | 89 | ct50s1→ceiling_tile_50s_1, pcc→popcorn_ceiling |
| Power Grid | 44 | pcab1→pupi_crossarm_1_brown, pullymount→clevis_mount |
| Technology | 34 | mbp→apple_macbook_pro, wg→spectrum_wireless_gateway |
| Lighting | 104 | ae115→american_electric_115_standard |
| Life Safety | 127 | firealarmcontrolpanel→fire_alarm_control_panel |
| Traffic Accessories | 245 | controlboxlarge→pole_mount_control_box_large |
| Traffic Signals | 120 | controllableverticalsolidsignal→vertical_traffic_signal_solid |
| Traffic Signs | 0 | (signs use blockstate texture overrides, no per-block models) |

### Phase 3: Organize Textures (SUBSTANTIALLY COMPLETE)

| Location | Files | Contents |
|---|---|---|
| textures/blocks/hvac/ | 30 | HVAC-exclusive textures with descriptive names |
| textures/blocks/novelties/ | 17 | Arcade screens, player skins, etc. |
| textures/blocks/novelties/shared_textures/ | 3 | arcade_cabinet, statue_base, water_bubbler |
| textures/blocks/buildingmaterials/ | 12 | Ceiling tiles with descriptive names |
| textures/blocks/powergrid/ | 4 | HV signs, visibility strips |
| textures/blocks/powergrid/shared_textures/ | 6 | crossarms, mountingbrackets, etc. |
| textures/blocks/technology/ | 35 | Speaker/device textures |
| textures/blocks/trafficsignals/ | 2+existing | Signal controller textures + TESR textures |
| textures/blocks/trafficsigns/ | 451 | Sign textures with lang-derived names |
| textures/blocks/shared_textures/ | ~261 | Cross-subsystem textures (metals, signal colors, etc.) |

### Phase 4: Sign Texture Renaming (PARTIALLY COMPLETE)

154 sign textures renamed from cryptic MUTCD codes to lang-derived names:
- `800px-hidot_road_floods_during_high_tide.svg.png` → `road_floods_during_high_tide.png`
- `1920px-mutcd_cw1-4r.svg.png` → `curve_right.png`
- `absolutelynothingsign.png` → `absolutely_nothing.png`

~297 sign textures still have original names (the tex→block mapping didn't find matches
for textures shared across multiple sign blocks or with non-standard naming).

### Blockstate Format Audit (COMPLETE)

1,298 of 1,313 blockstates (98.9%) already use Forge format. The remaining 15 are
multipart fences that legitimately require multipart format. No migration needed.

---

## Remaining Work

### Variant Models (~517 remaining in flat dir)

These are auto-generated variant models from `AbstractBlockSetBasic`:
- `*_fence_post.json`, `*_fence_inventory.json`
- `*_slab_top.json`
- `*_stairs_inner.json`, `*_stairs_outer.json`

Plus shared model files referenced as parents by multiple subsystems. These could be
moved to their respective subsystem folders, but they're lower priority since they're
internal implementation details, not primary block models.

### Remaining Flat Textures (~30)

Mostly metal color variants used by fence/slab/stairs blocks. Could move to
`buildingmaterials/` or `shared_textures/`.

### shared_textures/ Cleanup (~261 files)

The `shared_textures/` folder is a catch-all for cross-subsystem textures. Could benefit
from further organization:
- Signal light colors (solidred, solidgreen, etc.) — could go to trafficsignals/shared_textures/
- Exit sign textures — could go to lifesafety/shared_textures/
- APS button textures — could go to trafficsignals/shared_textures/

### Additional Sign Texture Renaming

~297 sign textures in trafficsigns/ or shared_textures/ still have original cryptic names
(MUTCD codes, SVG file names). These need manual mapping or a smarter texture→block
tracing approach that handles blockstate texture overrides.
